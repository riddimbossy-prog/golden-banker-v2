# Predict2U Learning Intelligence Trilogy — v194 to v196

## v194 Model Governance
Champion/challenger validation, shadow mode, calibration, drift detection, data-quality gates and rollback controls.

## v195 Smart Consensus
Independent evidence grouping, correlated-family caps, exact-market agreement and conflict rejection.

## v196 Advanced Match Context
Evidence-only pre-publication assessment of schedule, competition, availability, motivation, environment and pattern integrity.

## Installation
Install only the v196 changed-files package to receive all three versions together. No new SQL, Edge Function, GitHub secret or VAPID key is required. Run the existing Live Scores or full data workflow once after deployment, then open Admin → Model Governance, Smart Consensus and Match Context.

## Validation
- JavaScript syntax: passed
- Model self-tests: 4 passed
- Repository preflight: 110 required files passed
- Performance budget: 0 errors, 0 warnings, 33 checks
- Playwright specs are included but were not run in this sandbox because `@playwright/test` is not installed.
- The inherited source snapshot does not contain generated `data.js` or `community.js`; changed-files installation preserves the live repository copies.
