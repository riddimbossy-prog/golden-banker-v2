name: Update Predicto data
# FULL fetch: fixtures, odds, standings, team stats, streaks (fetch-data.js),
# then real-xG enrichment (enrich-xg.js) and the branded daily image
# (generate-image.js). This is the SLOW one — can run 30-90+ minutes depending
# on how many leagues have games. Commits data.js / xg.js / today.png when done.
on:
  schedule:
    - cron: "0 5 * * *"     # once daily ~05:00 UTC (adjust to taste). Also
                            # triggerable manually below.
  workflow_dispatch: {}
permissions:
  contents: write
  pages: write
  id-token: write
# CONCURRENCY (fixed): this long fetch runs in its OWN group so a scheduled
# scores run, a publish, or a second data trigger can NEVER cancel it mid-fetch
# (that was killing the run at ~40 min before the commit step). cancel-in-progress
# is false: if a data run is already going, a new one waits instead of clobbering.
concurrency:
  group: "predicto-data"
  cancel-in-progress: false
jobs:
  build:
    runs-on: ubuntu-latest
    # generous ceiling so a big fixture day never gets timed-out mid-fetch.
    timeout-minutes: 150
    steps:
      - name: Check out the repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Inject secret keys into config.txt (preserve committed LEAGUES etc.)
        run: |
          # The repo's committed config.txt holds LEAGUES, SEASON, DAYS_*, budgets.
          # Overwriting it wipes LEAGUES and the fetcher finds no games. So we only
          # APPEND the secret API keys; the parser takes the LAST value per key, so
          # these fill in credentials while your committed settings stay intact.
          if [ ! -f config.txt ]; then
            echo "ERROR: config.txt missing from repo (must be committed with LEAGUES)."; exit 1
          fi
          {
            echo ""
            echo "API_KEY=${{ secrets.API_KEY }}"
            echo "STATS_API_KEY=${{ secrets.STATS_API_KEY }}"
          } >> config.txt
          echo "LEAGUES line present:"; grep -c "^LEAGUES=" config.txt || echo "0 — WARNING no LEAGUES"

      - name: Run the fetcher (fixtures, odds, standings, stats)
        run: node fetch-data.js || echo "fetch-data skipped/partial"

      - name: Enrich with real xG (TheStatsAPI)
        run: node enrich-xg.js || echo "xG enrichment skipped"

      - name: Generate daily branded image
        run: |
          npm install sharp --no-save || echo "sharp install issue"
          node generate-image.js || echo "image generation skipped"

      - name: Restore clean config.txt before commit (never commit the API key)
        run: |
          git checkout -- config.txt 2>/dev/null || rm -f config.txt

      - name: Commit updated data.js (if changed)
        run: |
          git config user.name "predicto-bot"
          git config user.email "bot@users.noreply.github.com"
          if [ -z "$(git status --porcelain data.js xg.js today.png today.svg daily/ 2>/dev/null)" ]; then
            echo "No changes."; exit 0
          fi
          git add data.js xg.js today.png today.svg daily/ 2>/dev/null
          git commit -m "Update Predicto data (fixtures + xG + image)"
          # Generated files — on a race with the scores workflow, keep OUR fresh copy
          # rather than dying on an unmergeable rebase conflict (the bug that blocked
          # the push before). Retry with backoff.
          for i in 1 2 3 4 5; do
            git fetch origin main
            if ! git rebase origin/main; then
              echo "Rebase conflict on generated files — keeping our fresh copies."
              git checkout --ours data.js xg.js today.png today.svg 2>/dev/null || true
              git checkout --ours daily/ 2>/dev/null || true
              git add data.js xg.js today.png today.svg daily/ 2>/dev/null || true
              GIT_EDITOR=true git rebase --continue || git rebase --abort
            fi
            if git push origin HEAD:main; then echo "pushed (attempt $i)."; break; fi
            echo "Push attempt $i failed (remote moved) — syncing and retrying..."; sleep $((5 * i))
          done
